import Button from '@mui/material/Button';
function AddUserLinkButton() {
	let button = (
		<div>
			<Button variant='outlined'>Add Link</Button>
		</div>
	);

	return <div>{button}</div>;
}

export { AddUserLinkButton };
