import Button from '@mui/material/Button';
function EditProfileCancelButton() {
	let button = (
		<div>
			<Button variant='outlined'>Cancel</Button>
		</div>
	);

	return <div>{button}</div>;
}

export { EditProfileCancelButton };
